<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Upload JSON Page">
    <title>${$global.meta.pageTitle || "Upload JSON"}</title>

    <!-- Copy or extend your CSS here -->
    <style>
      html,
      body {
        font-family: inter;
        display: flex;
        justify-content: center;
        align-items: center;
        color: #000;
        background: #E5E5E0;
      }
      code {
        color: #fc0;
       }
      a {
        color: #09c;
      }
    </style>
  </head>

  <body>
    <${input.content}/>
  </body>
</html>
